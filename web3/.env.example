# Copy to .env and fill in. Use the same deployer key you use in client/.env.local (PRIVATE_KEY).
RPC_URL=https://evmrpc-testnet.0g.ai

# Used by forge script broadcast (without 0x prefix is OK; forge accepts both)
PRIVATE_KEY=

# Additional factory owners (can be the same as deployer for local/solo testing)
ADDRESS_AS=
ADDRESS_PP=
